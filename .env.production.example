# Production Environment Variables for Radio Calico

# Database Configuration
POSTGRES_DB=radiocalico
POSTGRES_USER=radiocalico
POSTGRES_PASSWORD=CHANGE_THIS_TO_A_SECURE_PASSWORD

# nginx Configuration
NGINX_PORT=80

# Application Configuration
NODE_ENV=production

# Security Notes:
# 1. Copy this file to .env.production and update the values
# 2. NEVER commit .env.production to version control
# 3. Use a strong, unique password for POSTGRES_PASSWORD
# 4. Consider using Docker secrets for sensitive data in production
